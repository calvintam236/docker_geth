FROM debian:buster-slim

LABEL maintainer="calvintam236"
LABEL description="geth in Docker. Full node. Supports CPU mining."

WORKDIR /tmp/

ADD \
https://gethstore.blob.core.windows.net/builds/geth-linux-386-1.10.4-aa637fd3.tar.gz \
.

RUN \
tar -xvf geth-linux-386-1.10.4-aa637fd3.tar.gz \
&& mv geth-linux-386-1.10.4-aa637fd3/geth /usr/local/bin/ \
&& chmod +x /usr/local/bin/geth \
&& rm -r *

ENTRYPOINT ["geth"]
CMD ["--help"]
